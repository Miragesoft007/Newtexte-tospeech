# Web framework
gradio>=4.0.0
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# TTS engines
TTS>=0.22.0                # Coqui XTTS v2 — real voice cloning (CPU/GPU)
# espeak-ng must be installed at system level: sudo apt install espeak-ng
# voxcpm>=0.1.0            # Uncomment for VoxCPM2 voice cloning (needs GPU)

# Book parsers
python-docx>=1.1.0         # Word DOCX support
pdfplumber>=0.10.0         # PDF text extraction
PyPDF2>=3.0.0              # PDF fallback
ebooklib>=0.18             # EPUB support
beautifulsoup4>=4.12.0     # HTML parsing for EPUB
lxml>=5.0.0

# Audio
soundfile>=0.12.1          # WAV write (used with VoxCPM2)
numpy>=1.24.0

# Optional — VoxCPM2 dependencies (GPU required)
# torch>=2.5.0
# torchaudio>=2.5.0
